nav
	ul
		li.nav
			a(href="/" class="links") Home
		li.nav
			if (user)
				a(href="/user/profile" class="links") #{user.username}
		li.nav
			if (!user)
				a(href="/user/signup" class="links") Sign Up!
		li.nav
			if (!user)
				a(href="/user/login" class="links") Log in
		li.nav
			if (user)
				a(href="/event/bulletin" class="links") See Bulletin
		li.nav
			if (user)
				a(href="/event/postmessage" class="links") Post your message!	
		li.nav
			if (user)
				form(class="forms" method="POST" action="/user/logout")
					button(type='submit') Logout