doctype html
html
	head
		link(rel='stylesheet', href='/css/main.css', type='text/css')

	title Welcome
	style(type='text/css').

	body
		img(src="/images/logo.png" id="logo")
		div(class="navbar")
		nav
			ul
				//- img(src="images/logo.png" id="logo")
				li
					a(href="/" class="links") Home
				li
					if (user)
						a(href="/user/profile")
							img(src="/images/user.png" id="userid")
				li
					if (!user)
						a(href="/user/signup") Sign Up!
				li
					if (!user)
						a(href="/user/login") Log in
				li
					if (user)
						a(href="/event/activityList") See all Events
				li
					if (user)
						a(href="/event/createActivity") Create an event
				
				li
					if (user)
						form(class="forms" method="POST" action="/user/logout")
							button(type='submit') Logout